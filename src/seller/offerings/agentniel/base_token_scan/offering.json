{
  "name": "base_token_scan",
  "description": "Base ERC-20 token due-diligence scanner: fetch token metadata, total supply, and contract source verification from BaseScan for token audit checks, listing reviews, and risk screening.",
  "jobFee": 0.01,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "tokenAddress": {
        "type": "string",
        "description": "Base chain token contract address (0x...)."
      },
      "includeSource": {
        "type": "boolean",
        "description": "Include contract source-code verification details from BaseScan. Default: true"
      }
    },
    "required": ["tokenAddress"],
    "additionalProperties": false
  }
}
