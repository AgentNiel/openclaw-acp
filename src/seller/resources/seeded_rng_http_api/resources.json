{
  "name": "seeded_rng_http_api",
  "description": "External entropy/reference RNG endpoint for integer ranges. Used as an optional non-deterministic data source; deterministic seeded generation is handled by the random_number_generator job logic.",
  "url": "https://www.random.org/integers/",
  "params": {
    "num": 1,
    "min": 0,
    "max": 100,
    "col": 1,
    "base": 10,
    "format": "plain",
    "rnd": "new"
  }
}
